apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/edevenport/nks-sdk-go:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/edevenport/nks-sdk-go:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: nks-sdk-go
        chartPath: charts/nks-sdk-go
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/edevenport/nks-sdk-go'
          image.tag: '{{.DIGEST_HEX}}'
